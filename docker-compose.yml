services:
  kraken-dca:
    build: .
    container_name: kraken-dca
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./src:/app/src
      - ./data:/data